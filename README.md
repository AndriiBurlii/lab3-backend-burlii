# 🧪 Лабораторна робота №3  
## Тема: Розробка REST API з Flask та Docker

**Виконав:** Бурлій Андрій  
**Група:** ІО-32  
**Варіант:** 3  

---

## 🎯 Мета роботи
Розробити REST API для обліку витрат із використанням **Flask**, **SQLAlchemy**,  
**PostgreSQL** та **Docker Compose**.  
Реалізувати CRUD-функціонал для користувачів, категорій і витрат, забезпечити  
контейнеризований запуск і тестування через Postman.

---

## ⚙️ Використані технології
- Python 3.11  
- Flask 3.x  
- SQLAlchemy + Alembic  
- PostgreSQL 16  
- Docker Compose  
- Marshmallow (валідація даних)  
- Postman / curl  

---

## 🏗 Етапи виконання

### 1️⃣ Запуск середовища
Файл `docker-compose.yml` створює два контейнери:
- **db** — PostgreSQL 16  
- **api** — Flask-додаток  

Запуск:
```bash
docker-compose up --build
✅ Після запуску обидва контейнери працюють коректно.
📸 Рис. 1. Запущені контейнери у Docker Desktop
2️⃣ Міграції бази даних

Виконано ініціалізацію та оновлення схеми бази даних:

flask --app manage.py db init
flask --app manage.py db migrate -m "initial"
flask --app manage.py db upgrade


Результат — створені таблиці users, categories, expenses.

3️⃣ Перевірка ендпоінтів користувачів та категорій

GET /health — перевірка стану API (200 OK)

POST /api/users — створення користувача (201 або 409)

POST /api/categories — створення категорії (201 або 409)

GET /api/categories — перелік категорій (200 OK)

📸 Рис. 2. Тестування ендпоінтів користувачів та категорій


4️⃣ Робота з витратами

POST /api/expenses — створення нової витрати

POST /api/expenses з некоректними даними → 422 Unprocessable Entity

GET /api/expenses — отримання списку витрат

📸 Рис. 3. Створення витрати та обробка помилки валідації


📸 Рис. 4. Відповідь при запиті GET /api/expenses


📬 Postman колекція

До проєкту додано готову колекцію для тестування API:
Lab3_Burlii_IO32_V3.postman_collection.json

Колекція містить запити:

1. GET /health

2. POST /api/users

3. POST /api/categories

4. POST /api/expenses

5. GET /api/expenses

